The code should be self explanatory, hence I have not added additional documentation.

To run the tests:

Clone the repo
Go to the billingengine project location in the Terminal
Type in 'gradle' or 'gradle clean test' to run the tests